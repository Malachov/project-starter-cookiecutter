"""If python variables are stored in extra module, it's easy to overcome local function namespace and you can
edit variables from all the modules in a simple way."""
import pandas as pd

data = pd.DataFrame()